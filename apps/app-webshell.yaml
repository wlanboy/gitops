apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webshell
  namespace: argocd
spec:
  project: wlanboy

  source:
    repoURL: https://github.com/wlanboy/webpyshell.git
    targetRevision: main
    path: webpyshell-chart
    helm:
      releaseName: webshell
      valueFiles:
        - values.yaml

  destination:
    name: gmk
    namespace: webshell

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
