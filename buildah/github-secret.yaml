apiVersion: v1
kind: Secret
metadata:
  name: github-creds
  namespace: buildah
type: Opaque
stringData:
  # GitHub Personal Access Token (für private Repos), Token benötigt: repo scope
  # Wird als Basic-Auth in die Clone-URL eingesetzt (git-clone Init-Container)
  username: USERNAME
  token: REPLACE_ME
